pipeline {
    agent {
        label "linux"
    }
    environment {
    PROJECT_NAME = "Test-Project"
    OWNER_NAME   = "Elad"
    }
    stages {
        stage("Test-1") {
            steps {
                sh 'echo "Build Information" > build_info.txt'
            }
        }
    }
   post {
    failure {
        mail to: 'eladdaf@gmail.com', subject: 'Good Luck'
    }
   }
}
