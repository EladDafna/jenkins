pipeline {
    agent {
        label "linux"
    }
    stages {
        stage("Test-1") {
            steps {
                sh 'echo "Build Information" > build_info.txt'
            }
        }
    }
    post {
        success {
            emailext body: 'Build Passed\n\n${BUILD_URL}', subject: 'Build Success', to: 'eladdaf@gmail.com'
        }
    }
}
