pipeline {
    agent any
    
    stages {
        stage('Build Docker image') {
            steps {
                sh """
                sudo apt-get install docker.io
                  cd Docker
                  pwd
                  ls -la
                  docker build -t mydockerimage:latest .
                  docker images
                   """
            }
        }
    }
}
