pipeline {
    agent any
    stages {
        stage ('Build Docker image') {
            steps {
                script {
                    sh """
                       echo 'sudo apt install docker.io'
                       """
                       
                }
            }
        }
    }
}
